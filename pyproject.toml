[project]
name = "zizmor-pre-commit"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
    "zizmor==1.29.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
    "pytest>=9.0.0",
    "pyyaml>=6.0.2",
]
